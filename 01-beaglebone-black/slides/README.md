# BeagleBone Black presentation

`BeagleBone_Boot_Build_Flow.pptx` trình bày quy trình build Full Flow cho BeagleBone Black, ý nghĩa từng artefact, chuỗi boot, cách tạo image, flash microSD và các checkpoint debug qua UART.

